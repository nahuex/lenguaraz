# Credential-free dry run: the whole UI with fake captions from the bundled samples.
# cp examples/env/dry-run.env .env && docker compose up --build
ENGINE=fake
ADMIN_TOKEN=dry-run-token-change-me
LOG_LEVEL=INFO
STAGES_FILE=stages.yaml
