---
description: Break the plan into ordered, verifiable tasks (SDD step 4)
argument-hint: <feature number>
---
Feature: $ARGUMENTS.
Create `tasks.md` from the template: tasks ≤45 min, test-first ordering, each with Refs (FR/AC) and a Verify command, `[P]` if parallelizable, `[H]` if it needs me. End with the feature DoD. Estimate total time and compare with the milestone clock; if it doesn't fit, propose cuts from the ladder before starting. Commit `docs(tasks): NNN <name>`.
